### How to switch off case sentivity?
    > use `--ignore-case` options in the command line
### How to compare files using CRC?
    > use `rclone check` to compare src and dest (https://rclone.org/commands/rclone_check/)
